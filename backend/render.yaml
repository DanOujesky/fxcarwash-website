services:
  - type: web
    name: fxcarwash-backend
    env: node
    buildCommand: "npm install && npm run build"
    startCommand: "npm run start"
    envVars:
      - key: NODE_ENV
        value: production
